{
    "name": "mmorpg-isometric-client",
    "version": "1.0.0",
    "type": "module",
    "scripts": {
        "dev": "vite --port 3000",
        "build": "vite build",
        "typecheck": "tsc -p tsconfig.json --noEmit",
        "preview": "vite preview --port 3000"
    },
    "dependencies": {
        "phaser": "^3.70.0",
        "socket.io-client": "^4.7.2",
        "colyseus.js": "^0.15.0"
    },
    "devDependencies": {
        "typescript": "^5.5.4",
        "vite": "^5.0.0"
    }
}
